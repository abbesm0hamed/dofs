general {
    no_fade_in = false
    grace = 0
    disable_loading_bar = true
}

auth {
    fingerprint:enabled = true
    fingerprint:ready_message = (Scan fingerprint to unlock)
    fingerprint:present_message = Scanning fingerprint
    fingerprint:retry_delay = 1000
}

label {
    monitor =
    text = $FPRINTPROMPT
    color = rgba(e8edf2ff)
    font_size = 14
    font_family = "JetBrains Mono"
    position = 0, -150
    halign = center
    valign = center
}

label {
    monitor =
    text = $FPRINTFAIL
    color = rgba(f28fadff)
    font_size = 12
    font_family = "JetBrains Mono"
    position = 0, -180
    halign = center
    valign = center
}

background {
    monitor =
    path = ~/.config/backgrounds/current-blurry.jpg
    color = rgba(0f1115ff)
    blur_passes = 2
    blur_size = 7
}

input-field {
    monitor =
    size = 200, 50
    outline_thickness = 3
    dots_size = 0.33
    dots_spacing = 0.15
    dots_center = true
    outer_color = rgba(2a3240ff)
    inner_color = rgba(e8edf2ff)
    font_color = rgba(0f1115ff)
    fade_on_empty = true
    placeholder_text = <i>Input Password...</i>
    hide_input = false
    position = 0, -80
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] echo "$TIME"
    color = rgba(e8edf2ff)
    font_size = 64
    font_family = Iosevka Mono
    position = 0, 150
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] echo "$(date +'%A, %d %B')"
    color = rgba(aeb7c6ff)
    font_size = 22
    font_family = Iosevka Mono
    position = 0, 50
    halign = center
    valign = center
}
