general {
    no_fade_in = false
    grace = 0
    disable_loading_bar = true
}

auth {
    fingerprint:enabled = true
    fingerprint:ready_message = (Scan fingerprint to unlock)
    fingerprint:present_message = Scanning fingerprint
    fingerprint:retry_delay = 1000
}

label {
    monitor =
    text = $FPRINTPROMPT
    color = rgba({{colors.on_surface.default.hex_stripped}}ff)
    font_size = 14
    font_family = "JetBrains Mono"
    position = 0, -150
    halign = center
    valign = center
}

label {
    monitor =
    text = $FPRINTFAIL
    color = rgba({{colors.error.default.hex_stripped}}ff)
    font_size = 12
    font_family = "JetBrains Mono"
    position = 0, -180
    halign = center
    valign = center
}

background {
    monitor =
    path = ~/.config/backgrounds/current-blurry.jpg
    color = rgba({{colors.surface.default.hex_stripped}}ff)
    blur_passes = 2
    blur_size = 7
}

input-field {
    monitor =
    size = 200, 50
    outline_thickness = 3
    dots_size = 0.33
    dots_spacing = 0.15
    dots_center = true
    outer_color = rgba({{colors.outline.default.hex_stripped}}ff)
    inner_color = rgba({{colors.on_surface.default.hex_stripped}}ff)
    font_color = rgba({{colors.surface.default.hex_stripped}}ff)
    fade_on_empty = true
    placeholder_text = <i>Input Password...</i>
    hide_input = false
    position = 0, -80
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] echo "$TIME"
    color = rgba({{colors.on_surface.default.hex_stripped}}ff)
    font_size = 64
    font_family = Iosevka Mono
    position = 0, 150
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:1000] echo "$(date +'%A, %d %B')"
    color = rgba({{colors.on_surface_variant.default.hex_stripped}}ff)
    font_size = 22
    font_family = Iosevka Mono
    position = 0, 50
    halign = center
    valign = center
}
