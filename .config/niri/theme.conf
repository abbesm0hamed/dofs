// Niri Theme Configuration - Catppuccin Mocha
// This file is sourced by config.kdl

// Color definitions from Catppuccin Mocha
// Base colors
default-column-width { proportion 0.5; }

// Window decorations will use colors from the layout section in config.kdl
// The theme colors are applied via the border settings

// Additional theme-specific settings can be added here
// Currently using inline theme definitions in config.kdl for better visibility
