workspace "browser" {
    // Workspace 1: Web browsers and browsing
}
workspace "terminal" {
    // Workspace 2: Terminal applications and development
}
workspace "communication" {
    // Workspace 3: Communication and messaging apps
}

input {
    workspace-auto-back-and-forth
    
    keyboard {
        xkb {
            layout "us,fr"
            options "compose:ralt"
        }
        numlock
    }

    touchpad {
        tap
        natural-scroll
    }

    mouse {
        accel-speed 0.0
        accel-profile "flat"
    }

    trackpoint {
        natural-scroll
    }

    focus-follows-mouse max-scroll-amount="0%"
}

output "eDP-1" {
    mode "1920x1080@144"
    scale 1.0
    transform "normal"
    position x=1920 y=0  // Move laptop to the right
}
output "HDMI-A-1" {
    mode "1920x1080@144"
    scale 1.0
    transform "normal"
    position x=0 y=0
}

overview {
    zoom 0.45
    workspace-shadow {
        off
    }
    backdrop-color "#111318"
}

layer-rule {
    match namespace="wallpaper"
    match namespace="swaybg"
    place-within-backdrop true
}

layer-rule {
    match namespace="swww"
}

cursor {
    xcursor-theme "Adwaita"
    xcursor-size 24
    hide-when-typing
    hide-after-inactive-ms 10000
}

layout {
    gaps 0
    background-color "#111318"
    center-focused-column "never"

    preset-column-widths {
        proportion 0.33333
        proportion 0.5
        proportion 0.66667
        proportion 1.0
    }

    default-column-width { proportion 0.5; }

    focus-ring {
        off
    }

    border {
        width 1
        active-color "#89b4fa"
        inactive-color "#45475a"
        urgent-color "#f38ba8"
    }

    shadow {
        softness 30
        spread 5
        offset x=0 y=5
    }

    struts {}
}

spawn-at-startup "bash" "-c" "~/.config/niri/scripts/autostart.sh"

prefer-no-csd
screenshot-path "~/Pictures/Screenshots/Screenshot from %Y-%m-%d %H-%M-%S.png"

animations {
    // slowdown 1

    workspace-switch {
        spring damping-ratio=1.0 stiffness=1000 epsilon=0.0001
    }

    window-open {
        spring damping-ratio=0.8 stiffness=1000 epsilon=0.0001
    }

    window-close {
        duration-ms 150
        curve "ease-out-quad"
    }

    horizontal-view-movement {
        spring damping-ratio=1.0 stiffness=800 epsilon=0.0001
    }

    window-movement {
        spring damping-ratio=1.0 stiffness=800 epsilon=0.0001
    }
}

window-rule {
    match title=r#".*[Pp]rivate.*|.*[Ii]ncognito.*"#
    block-out-from "screen-capture"
}

window-rule {
    match app-id=r#"^org\.wezfurlong\.wezterm$"#
    default-column-width {}
}

window-rule {
    match app-id=r#"^(code|Code|VSCodium|vscodium|Windsurf|windsurf|Cursor|cursor|discord|Antigravity|steam|Steam|ghostty|Ghostty|com\.mitchellh\.ghostty)$"#
    default-column-width { proportion 1.0; }
    open-maximized true
}

window-rule {
    match app-id=r#"^(zen|zen-browser|app\.zen_browser\.zen|firefox|chromium-browser|google-chrome|brave)$"#
    open-on-workspace "browser"
    default-column-width { proportion 1.0; }
    open-maximized true
}

window-rule {
    match app-id=r#"^(Slack|discord|chrome-web.whatsapp.com__-Default|telegram|signal)$"#
    open-on-workspace "communication"
    open-maximized true
}

window-rule {
    match app-id=r#"firefox$"# title="^Picture-in-Picture$"
    open-floating true
}

window-rule {
    match app-id="pavucontrol"
    match app-id="blueman-manager"
    match app-id="nm-connection-editor"
    match app-id="org.gnome.PowerStats"
    match app-id="xdg-desktop-portal-gtk"
    match app-id="system-config-printer"
    match app-id="com.gabm.satty"
    open-floating true
}

window-rule {
    match app-id=r#"^org\.keepassxc\.KeePassXC$"#
    match app-id=r#"^org\.gnome\.World\.Secrets$"#
    block-out-from "screen-capture"
}

window-rule {
    geometry-corner-radius 0
    clip-to-geometry true
}

hotkey-overlay {
    skip-at-startup
}

binds {
    Mod+Return { spawn "ghostty"; }
    Mod+T { spawn "kitty"; }
    Alt+Z { spawn "flatpak" "run" "app.zen_browser.zen"; }
    Mod+Space { spawn "fuzzel"; }    
    Alt+Ctrl+L {
        spawn "swaylock"
    }
    Alt+E { spawn "nautilus"; }
    Alt+T { spawn "zathura"; }  // Quick PDF viewer launch
    Alt+A { spawn "chromium-browser" "--app=https://chatgpt.com"; }
    Alt+Shift+C { spawn "chromium-browser" "--app=https://claude.ai/new"; }
    Alt+D { spawn "chromium-browser" "--app=https://discord.com/app"; }
    Alt+W { spawn "chromium-browser" "--app=https://web.whatsapp.com"; }
    Alt+N { spawn "chromium-browser" "--app=https://notion.so"; }
    Alt+F { spawn "chromium-browser" "--app=https://figma.com"; }

    // Screenshots
    // Built-in screenshot fallbacks (basic functionality)
    Mod+Shift+S { screenshot-screen; }
    Mod+Shift+V { screenshot-window; }
    Mod+Print { screenshot; }
    Print { spawn "~/.config/niri/scripts/screenshot.sh" "region"; }  // Region screenshot with editing
    Shift+Print { spawn "~/.config/niri/scripts/screenshot.sh" "window"; }  // Window screenshot with editing
    Ctrl+Print { spawn "~/.config/niri/scripts/screenshot.sh" "screen"; }  // Full screen screenshot with editing
    
    // Screen Recording with Audio Support
    Alt+Print { spawn "~/.config/niri/scripts/screencast.sh" "region"; }  // Record region (no audio)
    Ctrl+Alt+Print { spawn "~/.config/niri/scripts/screencast.sh" "screen"; }  // Record full screen (no audio)
    Alt+Shift+Print { spawn "~/.config/niri/scripts/screencast.sh" "region-audio"; }  // Record region with audio
    Ctrl+Shift+Print { spawn "~/.config/niri/scripts/screencast.sh" "screen-audio"; }  // Record full screen with audio
    
    // Audio-only recording
    Alt+Ctrl+A { spawn "~/.config/niri/scripts/screencast.sh" "audio-only"; }  // Record audio only
    
    // Stop any active recording
    Alt+Ctrl+Shift+R { spawn "~/.config/niri/scripts/screencast.sh" "stop"; }  // Stop recording
    
    // Clipboard Management
    Alt+Ctrl+V { spawn "~/.config/niri/scripts/clipboard-manager.sh"; }  // Show clipboard history
    
    // System Controls
    Alt+Space { spawn "~/.config/niri/scripts/system-controls.sh"; }
    Alt+Ctrl+Shift+Space { spawn "bash" "~/.config/niri/scripts/theme-menu.sh"; }
    Alt+S { spawn "~/.config/niri/scripts/settings-menu.sh"; }
    Alt+L { spawn "~/.config/waybar/scripts/power-menu.sh"; }
    
    // Language switching
    Alt+Mod+Ctrl+Space { switch-layout "next"; }

    // Example volume keys mappings for PipeWire & WirePlumber.
    // The allow-when-locked=true property makes them work even when the session is locked.
    XF86AudioMicMute allow-when-locked=true { spawn "pactl" "set-source-mute" "@DEFAULT_SOURCE@" "toggle"; }
    XF86AudioMute allow-when-locked=true { spawn "pactl" "set-sink-mute" "@DEFAULT_SINK@" "toggle"; }
    XF86AudioRaiseVolume allow-when-locked=true { spawn "wpctl" "set-volume" "-l" "1.0" "@DEFAULT_AUDIO_SINK@" "5%+"; }
    XF86AudioLowerVolume allow-when-locked=true { spawn "wpctl" "set-volume" "@DEFAULT_AUDIO_SINK@" "5%-"; }
    XF86MonBrightnessDown allow-when-locked=true { spawn "bash" "-c" "current=$(brightnessctl g); max=$(brightnessctl m); min=$((max * 5 / 100)); new=$((current - max * 5 / 100)); [ $new -lt $min ] && new=$min; brightnessctl s $new"; }
	XF86MonBrightnessUp   allow-when-locked=true { spawn "brightnessctl" "set" "+5%"; }

    Mod+Tab repeat=false { toggle-overview; }

    Mod+Shift+Q { close-window; }

    Mod+H  { focus-column-left; }
    Mod+J  { focus-window-or-workspace-down; }
    Mod+K  { focus-window-or-workspace-up; }
    Mod+L { focus-column-right; }
    Mod+Shift+H  { move-column-left; }
    Mod+Shift+J  { move-window-down-or-to-workspace-down; }
    Mod+Shift+K  { move-window-up-or-to-workspace-up; }
    Mod+Shift+L { move-column-right; }

    Mod+Home { focus-column-first; }
    Mod+End  { focus-column-last; }
    Mod+Ctrl+Home { move-column-to-first; }
    Mod+Ctrl+End  { move-column-to-last; }

    Ctrl+Shift+H { focus-monitor-left; }
    Ctrl+Shift+L { focus-monitor-right; }
    Ctrl+Shift+K { focus-monitor-up; }
    Ctrl+Shift+J { focus-monitor-down; }

    Mod+Shift+Ctrl+Left  { move-column-to-monitor-left; }
    Mod+Shift+Ctrl+Down  { move-column-to-monitor-down; }
    Mod+Shift+Ctrl+Up    { move-column-to-monitor-up; }
    Mod+Shift+Ctrl+Right { move-column-to-monitor-right; }
    Mod+Shift+Ctrl+H     { move-column-to-monitor-left; }
    Mod+Shift+Ctrl+J     { move-column-to-monitor-down; }
    Mod+Shift+Ctrl+K     { move-column-to-monitor-up; }
    Mod+Shift+Ctrl+L     { move-column-to-monitor-right; }

    // Alternatively, there are commands to move just a single window:
    // Mod+Shift+Ctrl+Left  { move-window-to-monitor-left; }
    // ...

    // And you can also move a whole workspace to another monitor:
    // Mod+Shift+Ctrl+Left  { move-workspace-to-monitor-left; }
    // ...

    Mod+W { toggle-column-tabbed-display; }
    
    Mod+Alt+J      { focus-workspace-down; }
    Mod+Alt+K        { focus-workspace-up; }
    Mod+Ctrl+K { move-window-to-workspace-up; }
    Mod+Ctrl+J { move-window-to-workspace-down; }
    // Mod+Ctrl+J { move-column-to-workspace-down; }
    // Mod+Ctrl+K   { move-column-to-workspace-up; }

    Mod+Shift+U         { move-workspace-down; }
    Mod+Shift+I         { move-workspace-up; }

    // You can bind mouse wheel scroll ticks using the following syntax.
    // These binds will change direction based on the natural-scroll setting.

    // To avoid scrolling through workspaces really fast, you can use
    // the cooldown-ms property. The bind will be rate-limited to this value.
    // You can set a cooldown on any bind, but it's most useful for the wheel.
    Mod+WheelScrollDown      cooldown-ms=150 { focus-workspace-down; }
    Mod+WheelScrollUp        cooldown-ms=150 { focus-workspace-up; }
    Mod+Ctrl+WheelScrollDown cooldown-ms=150 { move-column-to-workspace-down; }
    Mod+Ctrl+WheelScrollUp   cooldown-ms=150 { move-column-to-workspace-up; }

    Mod+WheelScrollRight      { focus-column-right; }
    Mod+WheelScrollLeft       { focus-column-left; }
    Mod+Ctrl+WheelScrollRight { move-column-right; }
    Mod+Ctrl+WheelScrollLeft  { move-column-left; }

    // Usually scrolling up and down with Shift in applications results in
    // horizontal scrolling; these binds replicate that.
    Mod+Shift+WheelScrollDown      { focus-column-right; }
    Mod+Shift+WheelScrollUp        { focus-column-left; }
    Mod+Ctrl+Shift+WheelScrollDown { move-column-right; }
    Mod+Ctrl+Shift+WheelScrollUp   { move-column-left; }

    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }
    Mod+6 { focus-workspace 6; }
    Mod+7 { focus-workspace 7; }
    Mod+8 { focus-workspace 8; }
    Mod+9 { focus-workspace 9; }
    Mod+Shift+1 { move-window-to-workspace 1; }
    Mod+Shift+2 { move-window-to-workspace 2; }
    Mod+Shift+3 { move-window-to-workspace 3; }
    Mod+Shift+4 { move-window-to-workspace 4; }
    Mod+Shift+5 { move-window-to-workspace 5; }
    Mod+Shift+6 { move-window-to-workspace 6; }
    Mod+Shift+7 { move-window-to-workspace 7; }
    Mod+Shift+8 { move-window-to-workspace 8; }
    Mod+Shift+9 { move-window-to-workspace 9; }

    // Alternatively, there are commands to move just a column:
    // Alt+Shift+1 { move-column-to-workspace 1; }

    // Switches focus between the current and the previous workspace.
    Alt+Tab { focus-workspace-previous; }

    Mod+BracketLeft  { consume-or-expel-window-left; }
    Mod+BracketRight { consume-or-expel-window-right; }

    // Consume one window from the right to the bottom of the focused column.
    Mod+Comma  { consume-window-into-column; }
    // Expel the bottom window from the focused column to the right.
    Mod+Period { expel-window-from-column; }

    Mod+R { switch-preset-column-width; }
    Mod+Shift+R { switch-preset-window-height; }
    Mod+Ctrl+R { reset-window-height; }
    Mod+F { maximize-column; }
    Mod+Shift+F { fullscreen-window; }

    // Makes the column "fill the rest of the space".
    Mod+Ctrl+F { expand-column-to-available-width; }

    Mod+C { center-column; }

    // Center all fully visible columns on screen.
    Mod+Ctrl+C { center-visible-columns; }

    // set-column-width "100" will make the column occupy 200 physical screen pixels.
    Alt+Minus { set-column-width "-10%"; }
    Alt+Equal { set-column-width "+10%"; }

    // Finer height adjustments when in column with other windows.
    Alt+Shift+Minus { set-window-height "-10%"; }
    Alt+Shift+Equal { set-window-height "+10%"; }

    // Move the focused window between the floating and the tiling layout.
    Alt+V       { toggle-window-floating; }
    Alt+Shift+V { switch-focus-between-floating-and-tiling; }

    Mod+Escape allow-inhibiting=false { toggle-keyboard-shortcuts-inhibit; }

    // The quit action will show a confirmation dialog to avoid accidental exits.
    Mod+Shift+E { quit; }
    Ctrl+Alt+Delete { quit; }

    Mod+Shift+P { power-off-monitors; }
}
